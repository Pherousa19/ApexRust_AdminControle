{
  "name": "apex-rust-store",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute apex-rust-store-v2 --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute apex-rust-store-v2 --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "stripe": "^17.7.0",
    "ws": "^8.14.2"
  }
}
