{
  "name": "apex-rust-store",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute apex-rust-store --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute apex-rust-store --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "stripe": "^17.7.0",
    "ws": "^8.14.2"
  }
}
